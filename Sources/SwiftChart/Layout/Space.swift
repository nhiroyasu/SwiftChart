import Foundation

enum ChartAlign {
    /// 左から整列
//    case start(let space: Double)
    /// 中央から整列
//    case center(let space: Double)
    /// 右から整列
//    case end(let space: Double)
    /// 要素間のスペースを同じにする
    case between
    /// 要素間と両端のスペースを同じにする
    case around
}
